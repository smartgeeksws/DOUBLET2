<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lionsoft.doublet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PantallaPrincipal" />
        <activity
            android:name=".SplahScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PantallaJuego"/>
        <activity android:name=".PantallaPuntajes" />
        <activity android:name=".PantallaConfiguraciones" />
        <activity android:name=".PantallaInfo" />
        <activity android:name=".PantallaLogin"></activity>
    </application>

</manifest>